/** Must match `shielded_pool::TREE_DEPTH` in Move. */
export const TREE_DEPTH = 20;

/** Must match `shielded_pool::ROOT_HISTORY_LEN` in Move. */
export const ROOT_HISTORY_LEN = 128;

export const MODULE_NAME = "shielded_pool";
